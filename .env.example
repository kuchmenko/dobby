# Copy to .env (gitignored) and uncomment values as needed for local dev.

# Verbose tracing output — matches tracing-subscriber's EnvFilter syntax.
# Examples:
#   RUST_LOG=debug
#   RUST_LOG=dobby=trace,tonic=info
#RUST_LOG=info

# Unwind backtraces on panic
#RUST_BACKTRACE=1
